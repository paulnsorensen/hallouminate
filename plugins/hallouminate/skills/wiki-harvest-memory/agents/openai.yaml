interface:
  display_name: "Harvest Agent Memory"
  short_description: "Curate Claude or Codex memory into the wiki"
  default_prompt: "Use $wiki-harvest-memory to review local agent memory and curate approved repository knowledge into the current hallouminate wiki."
policy:
  allow_implicit_invocation: false
