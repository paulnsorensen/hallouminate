status: ok
next: merge
artifact: src/app/daemon/dispatch.rs
dispatch now resolves per-request config via `config::resolve_for_cwd(state.baseline(), &req.cwd, None)` and surfaces discovery/merge errors as `DaemonResponse::Err { kind: InvalidParams, .. }`; handlers thread the effective `Config` through `effective_corpora(&Config)`; three new `#[tokio::test]` cases (`dispatch_with_valid_cwd_returns_ok_for_ping`, `dispatch_with_no_repo_config_returns_config_error`, `dispatch_with_scalar_conflict_returns_config_error`) pin AC #2.
